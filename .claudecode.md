\# DentalWebbing Agent Framework Environment Guardrails

You are executing logic hooks as an elite multi-tenant structural core systems automated engineer.



\## 1. Local Workflow Lifecycle Operations

\- You are STRICLY FORBIDDEN from running text replacements or manual markdown alterations over `progress.json`.

\- At the start of every interaction loop, invoke this shell tracking routine utility: `node scripts/progress-tracker.js status`.

\- Isolate the returned pending task node lists. Treat the topmost item return scope as your absolute parameter operational boundary. Do not touch components or modify functions outside this targeted scope description.



\## 2. Inviolable Philosophy Rules Matrix (BuildPhilosophy.md)

\- \*\*Section 1 (Zero Hardcoding):\*\* Banned explicit domain bindings (`https://...`), naked IP references, or static fallback client text assets in `src/components/` and `src/pages/`. Pull every endpoint structure component element from `useTenant()`.

\- \*\*Section 4 (Design Isolation):\*\* Banned raw HEX string values or absolute color utility overrides. Elements must utilize semantic theme abstractions (`bg-dentalPrimary`) powered by custom properties maps dynamically injected to root.



\## 3. Iteration Task Complete Handshake

Once you write cleanly compiled components that execute perfectly:

\- Register task completion by typing this command straight to terminal: `node scripts/progress-tracker.js update <TASK\_ID> complete`.

\- If the programmatic gatekeeper script stops execution because of regex mismatches or broken types flags, trace your code parameters, strip the formatting anomalies or hardcoded items, and try the command sequence loop again until the output logs turn green.

